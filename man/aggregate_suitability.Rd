% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_suitability.R
\name{aggregate_suitability}
\alias{aggregate_suitability}
\title{Aggregate species suitability over a polygon grid}
\usage{
aggregate_suitability(stars_obj, grid, fun = mean, na_rm = TRUE)
}
\arguments{
\item{stars_obj}{A \code{stars} object (e.g., from \code{vsc_predict_sdm_for_new_area}) with
a suitability attribute and a \code{species} dimension. Attribute must be \code{"suit"}
or \code{"suitability"}.}

\item{grid}{An \code{sf} polygon layer (POLYGON/MULTIPOLYGON) defining zones.}

\item{fun}{Summary function applied within polygons (default \code{mean}).}

\item{na_rm}{Logical; remove NAs inside \code{fun} (default \code{TRUE}).}
}
\value{
A \code{stars} object with attribute \code{"suitability"}, polygon-based spatial
dimension (from \code{grid}) and a \code{species} dimension.
}
\description{
Aggregates per-cell suitability for each species onto a user-provided \strong{polygon}
grid (zonal statistics), computing a summary (default: mean) within each polygon.
This collapses the \verb{(x, y)} spatial dimensions into a \code{cell} (polygon) dimension.
}
