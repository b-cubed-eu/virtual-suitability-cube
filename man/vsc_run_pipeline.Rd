% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_pipeline.R
\name{vsc_run_pipeline}
\alias{vsc_run_pipeline}
\title{Run the vscube SDM pipeline}
\usage{
vsc_run_pipeline(
  occ_file,
  iso3,
  month = 5,
  background_points = 10000,
  variables = c("tmin", "tmax", "prec", "tavg", "wind"),
  res = 0.5,
  version = "2.1",
  path = tempdir(),
  predictors = NULL,
  verbose = TRUE
)
}
\arguments{
\item{occ_file}{Path to GBIF TSV/TXT (scientificName, decimalLatitude, decimalLongitude, year).}

\item{iso3}{ISO3 country code for climate (e.g., "NLD").}

\item{month}{Integer 1–12: which month to use as predictors (default 5 = May).}

\item{background_points}{Integer background size for MaxEnt (default 10000).}

\item{variables}{Character vector of WorldClim variables (default: c("tmin","tmax","prec","tavg","wind")).}

\item{res, version, path}{Passed to \code{vsc_build_worldclim()}.}

\item{predictors}{Optional character vector: predictor names (default all).}

\item{verbose}{Logical; print progress (default TRUE).}
}
\value{
A list with components:
\itemize{
\item \code{climate}: list(stars_climate, predictors)
\item \code{occ}: cleaned occurrence table
\item \code{species_list}: split occurrences by species (list of data.frames)
\item \code{sdms}: list(models, predictions)
}
}
\description{
Download climate, read & split occurrences, train MaxEnt for all species, and predict suitability.
}
